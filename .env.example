# ============================================================
# LoopyMart Frontend - Environment Variables
# ============================================================
# Copy this file to .env.local and fill in the values.
# IMPORTANT: Never commit .env.local to version control!
# ============================================================

# ---------- Development ----------
# API Backend URL (for Vite dev server proxy)
VITE_API_URL=http://127.0.0.1:8001

# ---------- Production (reverse proxy / Option A) ----------
# When using a single host that proxies /api and /static to the backend,
# use same-origin paths so the browser never hits the backend port:
#   VITE_API_BASE_URL=/api
#   VITE_STATIC_URL=   (or /static for uploads)
# Build: npm run build (no need to set VITE_API_URL for build)
VITE_API_BASE_URL=/api
VITE_STATIC_URL=

# ============================================================
# Docker Compose Variables
# ============================================================
# These are only used by docker-compose.yml (not Vite).
# Copy this file to .env for Docker usage:
#   cp .env.example .env

# ── Security (CHANGE in production) ──
SECRET_KEY=dev-secret-key-change-in-production
POSTGRES_PASSWORD=loopymart_dev

# ── Seed admin account ──
ADMIN_EMAIL=admin@loopymart.local
ADMIN_PASSWORD=admin123
ADMIN_NAME=Admin

# ── Networking ──
# Port nginx listens on — change to 8080 if 80 is in use
HTTP_PORT=80

# Allowed CORS origins (comma-separated)
CORS_ORIGINS=http://localhost,http://localhost:80

# ── Optional: Razorpay (leave blank for COD / dummy-pay only) ──
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# ── Optional: Ollama LLM (only used with --profile llm) ──
OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=mistral
