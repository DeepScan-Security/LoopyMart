# ============================================================
# Flipkart Clone Frontend - Environment Variables
# ============================================================
# Copy this file to .env.local and fill in the values.
# IMPORTANT: Never commit .env.local to version control!
# ============================================================

# API Backend URL (for Vite dev server proxy)
# Used during development to proxy /api requests
# Default: http://127.0.0.1:8001
VITE_API_URL=http://127.0.0.1:8001

# API Base URL (for axios client)
# In production, set this to your API server URL
# Default: /api (uses proxy in development)
VITE_API_BASE_URL=/api

# Static Files URL (for images, uploads)
# In production, set this to your static files server URL
# Default: empty (uses relative paths)
VITE_STATIC_URL=
