# ============================================================
# LoopyMart - Environment Variables
# ============================================================
# Copy this file to .env and configure as needed.
# Most settings have development defaults - only configure
# what you need to change.
# IMPORTANT: Never commit .env to version control!
# ============================================================

# ------------------------------------------------------------
# DATABASE SETTINGS
# ------------------------------------------------------------

# PostgreSQL Database (for user authentication)
# Default: SQLite (./app.db) for development
# For production, use PostgreSQL:
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/loopymart_users

# MongoDB (for products, categories, cart, orders)
# Default: mongodb://localhost:27017
# MONGODB_URL=mongodb://localhost:27017
# MONGODB_DB_NAME=loopymart

# ------------------------------------------------------------
# SECURITY SETTINGS
# ------------------------------------------------------------

# JWT Secret Key
# IMPORTANT: Change this in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
# Default: dev-secret-key-change-in-production (INSECURE - for dev only)
# SECRET_KEY=your-secure-random-key

# JWT Settings
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ------------------------------------------------------------
# APPLICATION SETTINGS
# ------------------------------------------------------------

# APP_NAME=LoopyMart
# DEBUG=false

# CORS Origins (comma-separated list of allowed origins)
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Static files directories (relative to backend folder)
# STATIC_DIR=static
# UPLOADS_DIR=static/uploads

# API Limits
# API_PRODUCTS_DEFAULT_LIMIT=50
# API_PRODUCTS_MAX_LIMIT=100

# Payment Settings
# MIN_PAYMENT_AMOUNT_PAISE=100

# ------------------------------------------------------------
# ADMIN SEED CREDENTIALS
# ------------------------------------------------------------
# An admin account is automatically created on first startup.
# Override the defaults below to use your own credentials.
# The account is created only if the email does not already exist.

# ADMIN_EMAIL=admin@something.com      # default: admin@something.com
# ADMIN_PASSWORD=admin123              # default: admin123 — CHANGE IN PRODUCTION!
# ADMIN_NAME=Admin                     # default: Admin

# ------------------------------------------------------------
# PAYMENT GATEWAY (Razorpay - Optional)
# ------------------------------------------------------------
# Get keys from https://dashboard.razorpay.com/
# If not set, checkout uses "Place Order" without online payment.

# RAZORPAY_KEY_ID=rzp_test_xxxx
# RAZORPAY_KEY_SECRET=your_secret
